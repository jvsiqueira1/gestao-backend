# Neon pooled connection (used by the Prisma client at runtime)
DATABASE_URL=postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/db?sslmode=require

# Neon direct connection (used by `prisma migrate deploy`)
DIRECT_URL=postgresql://user:pass@ep-xxx.region.aws.neon.tech/db?sslmode=require

JWT_SECRET=
FRONTEND_URL=http://localhost:3000
PORT=4000

# Single-user lock: somente este email pode logar/cadastrar
OWNER_EMAIL=joaovitorsc@gmail.com
OWNER_PASSWORD=
OWNER_NAME=João Vitor
